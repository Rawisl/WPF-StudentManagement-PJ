<UserControl x:Class="WPF_StudentManagement_Project.Views.TiepNhanUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:WPF_StudentManagement_Project.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodels:HocSinhViewModel/>
    </UserControl.DataContext>

    <Grid>
        <materialDesign:Card HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,20,0,0" Background="White" UniformCornerRadius="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Rectangle Grid.Column="1" HorizontalAlignment="Left" Height="360" Stroke="Black" VerticalAlignment="Center" Width="2" Fill="#FFDDDDDD" Grid.ColumnSpan="2"/>

                <StackPanel x:Name="ThongTinSP" Grid.Column="0" Margin="30,20">
                    <Label Content="Thông tin cơ bản" FontSize="25" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Label Content="Họ Tên:" FontWeight="SemiBold" FontSize="18"/>
                    <TextBox Text="{Binding HoTen, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Height="30"/>

                    <Label Content="Giới tính:" FontWeight="SemiBold" FontSize="18"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,15">
                        <RadioButton Content="Nam" IsChecked="{Binding IsNam}" Margin="0,0,20,0"/>
                        <RadioButton Content="Nữ" IsChecked="{Binding IsNu}"/>
                    </StackPanel>

                    <Label Content="Ngày sinh:" FontWeight="SemiBold" FontSize="18"/>
                    <DatePicker SelectedDate="{Binding NgaySinh, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" Height="30"/>

                    <TextBlock Text="{Binding TuoiErrorMessage}" Foreground="Red" FontWeight="Bold" Margin="0,5,0,0" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel x:Name="LienLacSP" Grid.Column="2" Margin="30,20">
                    <Label Content="Liên lạc" FontSize="25" FontWeight="Bold" Margin="0,0,0,15"/>

                    <Label Content="Địa chỉ:" FontWeight="SemiBold" FontSize="18"/>
                    <TextBox Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>

                    <Label Content="Email:" FontWeight="SemiBold" FontSize="18"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,0" Height="30"/>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,410,50,0">
            <Button Command="{Binding HuyCommand}" Content="Hủy" Width="80" Margin="0,0,10,0" Background="Gray" BorderBrush="Transparent"/>

            <Button Command="{Binding LuuCommand}" Content="Lưu" Width="80" Height="28">
                <Button.Style>
                    <Style TargetType="{x:Type Button}">
                        <Setter Property="Background" Value="#FF270EDC"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type Button}">
                                    <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="1" 
                                    CornerRadius="5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="LightGray"/>
                                <Setter Property="Foreground" Value="DarkGray"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>