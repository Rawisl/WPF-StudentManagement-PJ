<UserControl x:Class="WPF_StudentManagement_Project.Views.TiepNhanUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:WPF_StudentManagement_Project.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodels:HocSinhViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White" UniformCornerRadius="20" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Rectangle Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Stretch" Width="2" Fill="#F0F0F0" Margin="0,10"/>

                <StackPanel x:Name="ThongTinSP" Grid.Column="0" Margin="10,0,30,0">
                    <Label Content="Thông tin cơ bản" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="#FF1A237E"/>

                    <Label Content="Họ Tên: *" FontWeight="SemiBold" FontSize="16" Foreground="Gray"/>
                    <Border Background="#F8F9FA" CornerRadius="8" BorderBrush="#D2D6DC" BorderThickness="1" Margin="0,5,0,15" Height="40">
                        <TextBox Text="{Binding HoTen, UpdateSourceTrigger=PropertyChanged}" 
                                 BorderThickness="0" Background="Transparent" 
                                 VerticalContentAlignment="Center" Padding="10,0" FontSize="15"/>
                    </Border>

                    <Label Content="Giới tính:" FontWeight="SemiBold" FontSize="16" Foreground="Gray"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,15" Height="40" VerticalAlignment="Center">
                        <RadioButton Content="Nam" IsChecked="{Binding IsNam}" Margin="0,0,30,0" FontSize="15"/>
                        <RadioButton Content="Nữ" IsChecked="{Binding IsNu}" FontSize="15"/>
                    </StackPanel>

                    <Label Content="Ngày sinh:" FontWeight="SemiBold" FontSize="16" Foreground="Gray"/>
                    <Border Background="#F8F9FA" CornerRadius="8" BorderBrush="#D2D6DC" BorderThickness="1" Margin="0,5,0,0" Height="40">
                        <DatePicker SelectedDate="{Binding NgaySinh, UpdateSourceTrigger=PropertyChanged}" 
                                    BorderThickness="0" Background="Transparent" 
                                    VerticalContentAlignment="Center" Padding="10,0" FontSize="15"/>
                    </Border>

                    <TextBlock Text="{Binding TuoiErrorMessage}" Foreground="#FF4757" FontWeight="Bold" Margin="0,8,0,0" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel x:Name="LienLacSP" Grid.Column="2" Margin="30,0,10,0">
                    <Label Content="Liên lạc" FontSize="24" FontWeight="Bold" Margin="0,0,0,15" Foreground="#FF1A237E"/>

                    <Label Content="Địa chỉ: *" FontWeight="SemiBold" FontSize="16" Foreground="Gray"/>
                    <Border Background="#F8F9FA" CornerRadius="8" BorderBrush="#D2D6DC" BorderThickness="1" Margin="0,5,0,15" Height="70">
                        <TextBox Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged}" 
                                 BorderThickness="0" Background="Transparent" 
                                 TextWrapping="Wrap" AcceptsReturn="True" Padding="10,5" FontSize="15"/>
                    </Border>

                    <Label Content="Email:" FontWeight="SemiBold" FontSize="16" Foreground="Gray"/>
                    <Border Background="#F8F9FA" CornerRadius="8" BorderBrush="#D2D6DC" BorderThickness="1" Margin="0,5,0,15" Height="40">
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                 BorderThickness="0" Background="Transparent" 
                                 VerticalContentAlignment="Center" Padding="10,0" FontSize="15"/>
                    </Border>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Command="{Binding HuyCommand}" Content="Hủy" Width="100" Height="40" Margin="0,0,15,0" Background="#95A5A6" BorderBrush="Transparent" Foreground="White" Cursor="Hand" FontSize="16" FontWeight="Bold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Command="{Binding LuuCommand}" Content="Lưu học sinh" Width="140" Height="40" Background="#270EDC" Foreground="White" BorderBrush="Transparent" Cursor="Hand" FontSize="16" FontWeight="Bold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#9E9E9E"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>