<UserControl x:Class="WPF_StudentManagement_Project.Views.NhapDiemUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_StudentManagement_Project.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             xmlns:vm="clr-namespace:WPF_StudentManagement_Project.ViewModels"
             xmlns:helpers="clr-namespace:WPF_StudentManagement_Project.Services">

    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="5" Margin="2,0,2,0"/>
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D2D6DC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0,30,0"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"/>

                            <ToggleButton x:Name="toggleButton" BorderBrush="Transparent" Background="Transparent"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent" CornerRadius="8">
                                            <Path x:Name="arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="Gray" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="10,0,30,0" VerticalAlignment="Center"/>

                            <Popup Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="White" BorderBrush="#D2D6DC" BorderThickness="1" Margin="0,2,0,0" CornerRadius="4">
                                    <ScrollViewer Margin="0,4,0,4" MaxHeight="150">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#8278E6"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1A73E8"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


    </UserControl.Resources>

    <Grid Background="#F5F6FA" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Nhập bảng điểm môn" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDDDDD" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock 
                        Text="Lớp" 
                        FontWeight="SemiBold" 
                        Margin="0,0,0,5" 
                        Foreground="Gray"/>
                    <ComboBox Height="35" 
                              Style="{StaticResource RoundedComboBox}" 
                              ItemContainerStyle="{StaticResource RoundedComboBoxItem}"
                              ItemsSource="{Binding DanhSachLop}"
                              SelectedItem="{Binding LopDuocChon}"
                              DisplayMemberPath="TenLop"/>

                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,20,0">
                    <TextBlock Text="Môn học" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Gray"/>
                    <ComboBox Height="35" 
                              Style="{StaticResource RoundedComboBox}" 
                              ItemContainerStyle="{StaticResource RoundedComboBoxItem}"
                              ItemsSource="{Binding DanhSachMon}"
                              SelectedItem="{Binding MonDuocChon}"
                              DisplayMemberPath="TenMonHoc">
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,20,0">
                    <TextBlock Text="Học kỳ" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="Gray"/>
                    <ComboBox Height="35" 
                              Style="{StaticResource RoundedComboBox}" 
                              ItemContainerStyle="{StaticResource RoundedComboBoxItem}"
                              ItemsSource="{Binding DanhSachHocKy}"
                              SelectedItem="{Binding HocKyDuocChon}">
                    </ComboBox>
                </StackPanel>

                <Button 
                    x:Name="GetListButton" 
                    Grid.Column="3" 
                    Content="Lấy danh sách" 
                    FontSize="16" 
                    Background="#FF270EDC" 
                    Foreground="White" 
                    Height="40" 
                    Width="150" 
                    Cursor="Hand" 
                    VerticalAlignment="Bottom" 
                    Command="{Binding LayDanhSachCommand}">
                    <Button.Resources>
                        <Style TargetType="{x:Type Border}">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Color="#DDDDDD" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Nhập điểm môn Toán - Lớp 10A1 - Học kỳ 1" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button 
                        Content="Lưu bảng điểm" 
                        Background="#00B894" 
                        Foreground="White" 
                        HorizontalAlignment="Right" 
                        Height="40" 
                        Width="150" 
                        Cursor="Hand"
                        Command="{Binding LuuBangDiemCommand}">
                        <Button.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>

                <DataGrid Grid.Row="1"
                          AutoGenerateColumns="False"
                          ItemsSource="{Binding DanhSachHocSinh}"
                          CanUserAddRows="False" 
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeight="50"
                          ColumnHeaderHeight="45"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="STT" Binding="{Binding Stt}" IsReadOnly="True" Width="80"/>

                        <DataGridTextColumn Header="Họ và tên" Binding="{Binding HoTen}" IsReadOnly="True" Width="*" />

                        <DataGridTextColumn Header="Điểm 15'" 
                                            Binding="{Binding Diem15Phut}" 
                                            IsReadOnly="False" 
                                            Width="120">
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter 
                                        Property="helpers:TextBoxHelper.IsDecimalOnly" 
                                        Value="True"/>
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Điểm 1 tiết" Binding="{Binding Diem1Tiet}" IsReadOnly="False" Width="120">
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="helpers:TextBoxHelper.IsDecimalOnly" Value="True"/>
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Điểm TB" Binding="{Binding DiemTB,StringFormat={}{0:F1}}" IsReadOnly="True" Width="120" Foreground="Gray"/>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</UserControl>